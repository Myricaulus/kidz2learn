@page "/graphem"
@using Kidz2Learn.Components
@using Kidz2Learn.Model

@* Cutover onto TaskHost, same pattern as "/" (SilbenChallenge, see TASK_PRESENTATION_REDESIGN.md
   Phase 4c). SilbenMultipleChoiceView gates audio/title on the skill list (GraphemPhonem never had
   an "Anhören" button - it's a silent visual-discrimination exercise, not a listening one), so no
   new view was needed here. *@
<TaskHost Skills="@(new[] { Skill.GraphemPhonem })" />
